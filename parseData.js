const readline = require('readline');
const stream = require('stream');
const fs = require('fs');


module.exports = {
    getPackage(name) {
        if(!name) return "<p class='error-message'>error getting package</p>";

    }
};